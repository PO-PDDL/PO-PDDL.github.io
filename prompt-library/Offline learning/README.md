# Offline learning Prompt Index

This directory is the grouped version of the prompt library for:

`python -m POMDPDDL.offline_model_learning.learning_pipeline.cli`

It preserves the previously documented per-prompt folders and places them under the `Offline learning` group.
See the local `README.md` files inside each prompt folder for the English role, input, and output notes.
