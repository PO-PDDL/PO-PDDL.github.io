# Transition Program Repair

Repair the current `sample_next_state(state, action, rng)` implementation using the provided mismatch examples.
Do not rely on any benchmark or task name; use only the provided interface, schema, current program, and mismatches.
