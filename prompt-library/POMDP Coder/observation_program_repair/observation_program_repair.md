# Observation Program Repair

Repair the current `sample_observation(next_state, action, rng)` implementation using the provided mismatch examples.
Do not rely on any benchmark or task name; use only the provided interface, schema, current program, and mismatches.
